<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp"
    android:focusable="true"
    android:clickable="true"
    android:foreground="?android:attr/selectableItemBackground"
    >

    <ImageView
        android:id="@+id/coming_or_going"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_alignParentTop="true"
        android:layout_centerVertical="true"
        android:layout_marginEnd="16dp"
        android:contentDescription="outgoing"
        android:src="@drawable/ic_call_made_black_24dp" />

    <TextView
        android:id="@+id/request_info"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_toEndOf="@id/coming_or_going"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/request_date_text_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/request_info"
        android:layout_toEndOf="@id/coming_or_going"
        android:text="Date: "
        android:textStyle="bold" />

    <TextView
        android:id="@+id/request_date_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/request_info"
        android:layout_toEndOf="@id/request_date_text_label" />

</RelativeLayout>
